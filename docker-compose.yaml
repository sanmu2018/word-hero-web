services:
  word-hero-web:
    image: sanmu2018/word-hero-web:v0.0.1
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=${REACT_APP_API_URL}
    container_name: word-hero-web
    ports:
      - "3000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
    networks:
      - word-hero-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    environment:
      - NODE_ENV=production
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.word-hero-web.rule=Host(`localhost`)"
      - "traefik.http.services.word-hero-web.loadbalancer.server.port=80"

networks:
  word-hero-network:
    driver: bridge

volumes:
  word-hero-data:
    driver: local